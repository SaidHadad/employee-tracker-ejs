const config = {};

config.serverPort = 8080;

config.dbConfig = {
    user: "root",
    password: "S@muelinho1",
    host: "localhost",
    port: 3306,
    database: "nodejs",
};

module.exports = config;